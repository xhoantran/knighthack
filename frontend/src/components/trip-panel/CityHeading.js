import { Typography } from "@mui/material";

export const CityHeading = () => {
  return (
    <Typography
      sx={{
        fontWeight: "medium",
        fontSize: "1.2rem",
        mt: 3,
        mb: 1,
        textTransform: "uppercase",
      }}
    >
      City
    </Typography>
  );
};
